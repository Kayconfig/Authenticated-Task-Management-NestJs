import { DataSerializationInterceptor } from './data-serialization.interceptor';

describe('DataSerializationInterceptor', () => {
  it('should be defined', () => {
    expect(new DataSerializationInterceptor()).toBeDefined();
  });
});
